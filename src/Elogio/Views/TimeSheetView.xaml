<UserControl x:Class="Elogio.Views.TimeSheetView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignHeight="500" d:DesignWidth="600">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Employee name -->
        <TextBlock Grid.Row="0"
                   Text="{Binding EmployeeName}"
                   FontSize="16"
                   FontWeight="SemiBold"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,10"/>

        <!-- Header with navigation -->
        <StackPanel Grid.Row="1" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,15">
            <Button Content="&lt; Previous" Command="{Binding PreviousWeekCommand}" Padding="10,5" Margin="0,0,10,0"/>
            <Button Content="Today" Command="{Binding GoToTodayCommand}" Padding="10,5" Margin="0,0,10,0"/>
            <TextBlock Text="{Binding WeekInfo}" FontSize="18" FontWeight="Bold" VerticalAlignment="Center" Margin="10,0"/>
            <Button Content="Next &gt;" Command="{Binding NextWeekCommand}" Padding="10,5" Margin="10,0,0,0"/>
        </StackPanel>

        <!-- Loading indicator -->
        <TextBlock Grid.Row="2"
                   Text="Loading..."
                   HorizontalAlignment="Center"
                   VerticalAlignment="Center"
                   FontSize="14"
                   Foreground="Gray"
                   Visibility="{Binding IsLoading, Converter={StaticResource BoolToVisibilityConverter}}"/>

        <!-- Days list -->
        <Border Grid.Row="2"
                BorderBrush="#DDD"
                BorderThickness="1"
                CornerRadius="5"
                Padding="10"
                Visibility="{Binding IsLoading, Converter={StaticResource InverseBoolToVisibilityConverter}}">
            <ItemsControl ItemsSource="{Binding Days}">
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Border Padding="10,8" Margin="0,0,0,5"
                                Background="{Binding IsWeekend, Converter={StaticResource BoolToBackgroundConverter}}"
                                CornerRadius="3">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="60"/>
                                    <ColumnDefinition Width="100"/>
                                    <ColumnDefinition Width="*"/>
                                    <ColumnDefinition Width="80"/>
                                    <ColumnDefinition Width="80"/>
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Column="0" Text="{Binding Date}" FontWeight="SemiBold"/>
                                <TextBlock Grid.Column="1" Text="{Binding DayOfWeek}" Foreground="Gray"/>
                                <TextBlock Grid.Column="2" Text="{Binding ScheduleInfo}" Foreground="Gray" FontSize="12"/>
                                <TextBlock Grid.Column="3" Text="{Binding WorkedTime}" HorizontalAlignment="Right" FontWeight="SemiBold"/>
                                <TextBlock Grid.Column="4" Text="{Binding ExpectedTime, StringFormat='/ {0}'}" HorizontalAlignment="Right" Foreground="Gray"/>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </Border>

        <!-- Totals -->
        <Border Grid.Row="3"
                Background="#F5F5F5"
                CornerRadius="5"
                Padding="15"
                Margin="0,15,0,0"
                Visibility="{Binding IsLoading, Converter={StaticResource InverseBoolToVisibilityConverter}}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>

                <TextBlock Grid.Column="0" Text="Weekly Total" FontWeight="Bold" VerticalAlignment="Center"/>

                <StackPanel Grid.Column="1" Orientation="Horizontal" Margin="20,0">
                    <TextBlock Text="Worked: " Foreground="Gray"/>
                    <TextBlock Text="{Binding TotalWorked}" FontWeight="SemiBold"/>
                </StackPanel>

                <StackPanel Grid.Column="2" Orientation="Horizontal" Margin="20,0">
                    <TextBlock Text="Expected: " Foreground="Gray"/>
                    <TextBlock Text="{Binding TotalExpected}" FontWeight="SemiBold"/>
                </StackPanel>

                <StackPanel Grid.Column="3" Orientation="Horizontal" Margin="20,0">
                    <TextBlock Text="Balance: " Foreground="Gray"/>
                    <TextBlock Text="{Binding Balance}" FontWeight="Bold" Foreground="#2196F3"/>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Error message -->
        <TextBlock Grid.Row="4"
                   Text="{Binding ErrorMessage}"
                   Foreground="Red"
                   Margin="0,10,0,0"
                   TextWrapping="Wrap"
                   Visibility="{Binding ErrorMessage, Converter={StaticResource NullToVisibilityConverter}}"/>
    </Grid>
</UserControl>
